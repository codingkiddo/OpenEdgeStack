# Storage Design

OpenEdgeStack v0.1 uses in-memory storage.

Benefits:

- easy to run
- no external dependencies
- fast tests
- simple MVP

Future versions may add PostgreSQL, Redis, Kafka/Redpanda, and TimescaleDB.
